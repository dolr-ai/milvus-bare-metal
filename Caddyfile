vector.yral.com {
	# gRPC reverse proxy to Milvus
	reverse_proxy h2c://milvus-standalone:19530 {
		# Required for gRPC
		transport http {
			versions h2c 2
		}

		# Pass through ALL headers (important for gRPC metadata/auth)
		header_up * *
	}
}
